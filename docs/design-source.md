# Design source

The original outreach portfolio template was adapted from the tw93/Kami design system.

The active `get-work` sheets are now self-contained HTML/CSS files under:

- `outreach/base/`
- `outreach/prospects/`

The full Kami clone was moved out of this working directory to avoid search/context noise:

```text
/home/exedev/archive/kami
```

Use that archived copy only if we need to reference the original design system again.
